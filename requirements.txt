# MCP Knowledge Base Server - Core Dependencies (CPU-only PyTorch)
# Install with: pip install -r requirements.txt

# Specify PyTorch CPU-only index FIRST
--extra-index-url https://download.pytorch.org/whl/cpu

# MCP Framework
fastmcp>=0.1.0

# Vector DB and NLP (will use CPU PyTorch from above index)
lancedb>=0.3.0
sentence-transformers>=2.2.0
transformers>=4.21.0

# Specify CPU-only PyTorch explicitly
# The +cpu suffix forces CPU version
torch>=2.0.0

# Data processing
numpy>=1.21.0
pandas>=1.3.0
pyarrow>=10.0.0

# Utilities
pyyaml>=6.0
aiofiles>=23.0.0
chardet>=5.0.0
platformdirs>=3.0.0